# Base URL backend Laravel (tanpa trailing slash berlebihan; path /api disertakan di sini)
# Salin file ini ke `.env` dan sesuaikan:
#   cp .env.example .env
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
